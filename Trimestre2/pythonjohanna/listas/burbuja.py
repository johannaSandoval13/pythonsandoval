for x in range(len(aprobados)):
    for y in range(x+1,len(reprobados)):
        if aprobados[x]>aprobados[y]:
            aux=aprobados[x]
            lista[x]=lista[y]
            lista[y]=aux
print(aprobados)

for x in range(len(aprobados)):
    for y in range(x+1,len(reprobados)):
        if aprobados[x]>aprobados[y]:
            aux=aprobados[x]
            lista[x]=lista[y]
            lista[y]=aux
print(aprobados)
for x in range(len(aprobados)):
    for y in range(x+1,len(reprobados)):
        if aprobados[x]>aprobados[y]:
            aux=aprobados[x]
            lista[x]=lista[y]
            lista[y]=aux
print(aprobados)
for x in range(len(aprobados)):
    for y in range(x+1,len(reprobados)):
        if aprobados[x]>aprobados[y]:
            aux=aprobados[x]
            lista[x]=lista[y]
            lista[y]=aux
print(aprobados)